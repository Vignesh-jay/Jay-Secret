services:
  jay-secret:
    build: .

    container_name: jay-secret

    restart: unless-stopped

    ports:
      - "3001:3000"

    env_file:
      - .env

    volumes:
      - ./server/db:/app/server/db
